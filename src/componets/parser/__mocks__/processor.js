export const mockProcessor = {
    priority: 1,
    name: "mockProcessor@v1",
    test: jest.fn(),
    edit: jest.fn(),
    save: jest.fn()
}

export const mockPriorityProcessor = {
    priority: 2,
    name: "mockPriorityProcessor@v1",
    test: jest.fn(),
    edit: jest.fn(),
    save: jest.fn()
}